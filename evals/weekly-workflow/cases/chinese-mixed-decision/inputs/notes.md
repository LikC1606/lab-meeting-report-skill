# Synthetic example: 步态事件检测项目本周工作

- 日期：2026-08-11
- 组会：会前，汇报时间 8 分钟
- 当前目标：减少楼梯场景中的误报，同时保留现有召回表现。

上次组会行动：跑完 temporal smoothing `window_5` 的 seed-13、seed-29、seed-42。已经完成，结果在 `results.csv`。

本周工作：

- 修复传感器时间戳缺口导致预处理重复上一帧的问题。
- `window_5` 的三个随机种子都跑完了。
- 试了 `window_9`。seed-42 的 recall 是 0.71，而 `window_5` 同一随机种子是 0.84；不准备继续扩大 smoothing window。
- 人工看了 24 个误报，楼梯转身和停步后重新起步经常被识别成 gait event。类别定义还没有整理完成。

当前阻塞：医院外部验证数据没有导出权限。2026-08-08 已提交数据申请，目前没有批准时间。这个阻塞不影响本地误差分析，但会推迟外部验证。

需要组内讨论：下周是先做 synthetic perturbation stress test，还是把主要精力放在推动数据权限上。两个方向都可以，但本周没有优先级规则。

下周无论选择哪个方向，都要先完成误报分类表。目标是累计检查 60 个误报，记录场景、预测位置和人工类别；负责人是 Chen，截止时间 2026-08-18。
